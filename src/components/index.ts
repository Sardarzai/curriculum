/* ========================Exporting the components from the folder Start======================== */
export { default as Header } from './Header/Header';
export { default as Profile } from './Profile/Profile';
export { default as Footer } from './Footer/Footer';
export { default as CustomTimeline } from './Timeline/CustomTimeline';
export { default as AppButtons } from './Button/AppButtons';
export { default as Toggle } from './Toggle/Toggle';
export { default as CountTime } from './CountTime/CountTime';
/* ========================Exporting the components from the folder End!======================== */

/* 
Note: With this export we can import any component anywhere just to mention the folder
name of URL section like:

import { Footer, Header, Profile ...} from './components';

*/
